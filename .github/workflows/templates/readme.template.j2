# 🖥️ {{ data.name | upper }}

[![Status](https://img.shields.io/badge/Status-{{ data.status | default('Active') }}-blue)]()
[![Environment](https://img.shields.io/badge/Environment-{{ data.environment | default('Production') }}-blue)]()
[![Type](https://img.shields.io/badge/Type-{{ data.type | upper }}-blue)]()

{{ data.description }}

---

## 📌 System Information & Network

| Property | Value |
| :--- | :--- |
| **Hostname** | `{{ data.hostname | default(data.name) }}` |
| **IP Address** | `{{ data.ip_address }}` |
| **Proxmox Node** | `{{ data.proxmox_node | default('PVE') }}` |
| **VM / LXC ID** | `{{ data.vmid }}` |
| **OS** | `{{ data.os | default('Unknown') }}` |

---

## ⚙️ Resource Allocation (Terraform)

| Resource | Allocated Amount |
| :--- | :--- |
| **CPU Cores** | `{{ data.cores | default('Unknown') }} vCPU` |
| **RAM Memory** | `{{ data.memory_mb | default('Unknown') }} MB` |
| **Root Disk** | `{{ data.disk_size_gb | default('Unknown') }} GB` |
| **Storage Pool** | `{{ data.storage_name | default('Unkown') }}` |

---

## 🌐 Services & Exposed Ports

{% if data.ports %}
| Service Name | Port / Protocol | Access |
| :--- | :--- | :--- |
{% for port_info in data.ports -%}
| **{{ port_info.name }}** | `{{ port_info.port }}/{{ port_info.protocol | default('tcp') }}` | '{{ port_info.access }}' |
{% endfor -%}
{% else %}
*No public Web or API ports declared for this service.*
{% endif %}

{% if data.docker %}
---

### Running Docker Stacks
This node is responsible for managing the following Docker stacks:
{% for stack in data.docker -%}
- 📦 {{ stack }}
{% endfor -%}
{% endif %}

---

## 📄 Automated Maintenance
> 🤖 *This README was automatically generated by CI/CD Pipeline based on `metadata.yaml` and `terraform.tfvars`. Last updated on: {{ generation_date }}*