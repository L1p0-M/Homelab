---
- name: Apply base configuration to all nodes
  hosts: all
  gather_facts: true
  roles:
    - base

- name: Setup Docker on Docker hosts
  hosts: docker-hosts
  gather_facts: true
  roles:
    - docker_host
