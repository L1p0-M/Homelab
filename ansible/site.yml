---
- name: Apply base configuration to all nodes
  hosts: all:!excluded
  gather_facts: true
  roles:
    - role: base
      tags: [base]

- name: Setup Docker on Docker hosts
  hosts: docker_hosts
  gather_facts: true
  roles:
    - role: docker_host
      tags: [docker_host]

