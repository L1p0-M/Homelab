---
- name: Enable & Start cups/sane service
  ansible.builtin.service:
    name: "{{ item }}"
    enabled: true
    state: started
  loop:
    - "{{ cups_service_name }}"
    - "{{ sane_service_name }}"
