deploy_user: "{{ vault_deploy_user }}"
my_user: "{{ vault_my_user }}"
deploy_user_key: "{{ vault_deploy_key }}"
ansible_become_password: "{{ vault_ansible_become_password }}"
deploy_user_pass: "{{ vault_deploy_user_pass }}"
deploy_ssh_key: "{{ vault_deploy_ssh_key }}"
sops_key: "{{ vault_sops_key }}"
cloudflare_token: "{{ vault_cloudflare_token }}"
cloudflare_domain: "{{ vault_cloudflare_domain }}"

docker_stacks_dir: "/dockerconfig"
docker_trash_dir: "/dockerconfig/.trash"
docker_retention_days: 3
